# MoonTier 改造计划

## 目标

- 在源码工程中增加可控的开机自启动功能，优先恢复 Root 模式下已启用/默认的网络配置，不依赖打开 GUI。
- 兼容不具备 KernelSU、Magisk 或 APatch 标识，但 `su` 实际可用的类原生云手机 Root 环境。
- 移除 App 内置和自动同步的官方服务器入口及默认数据，保留用户自行维护服务器/节点的能力。
- 下载 EasyTier Core 时依次尝试国内 GitHub 代理镜像，全部失败后回退 GitHub 原始地址。
- 构建、签名并仅在 `10.126.126.66:5555` 与后续 `10.126.126.34:5555` 上验证，禁止操作其他 ADB 设备。

## 实施步骤

1. 基线与环境确认
   - 核对源码包名、版本、ABI、构建依赖与原始 APK 信息。
   - 检查本机 JDK 17、Android SDK/NDK、Gradle、ADB、签名工具；缺失项仅下载到 MoonTier 工作范围或用户级工具目录。
   - 连接 `10.126.126.66:5555` 时所有命令显式使用 `adb -s 10.126.126.66:5555`，先做只读 Root 能力与现有 App 状态探测。

2. Root 兼容性
   - 将 Root 判断收敛为“能否执行受控 `su` 命令并确认 UID 0”，不以 Root 管理器品牌或固定目录作为必要条件。
   - 兼容常见 `su -c`、交互式 `su`/厂商预置 Root shell 差异，并保留超时、输出和失败诊断。
   - 在 Root 命令执行、Core 安装目录、后台 manager 启动路径上检查 SELinux、PATH、文件权限和进程存活逻辑。

3. 开机自启动
   - 增加持久化“开机自启动”设置和 Compose 开关，默认关闭，避免安装后未经用户选择自动联网。
   - 注册 `BOOT_COMPLETED`/`LOCKED_BOOT_COMPLETED`（按存储可用性处理）接收器；收到开机事件后读取设置并启动独立后台组件。
   - Root 模式下恢复上次应运行或默认配置，启动/附着共享 Root manager 与 EasyTier Core；已经运行时保持幂等，不重复拉起。
   - 对 Android 后台启动限制使用短生命周期前台服务承接恢复任务，并提供通知与错误日志；VPN 模式不在无授权条件下强行拉起。

4. 删除官方服务器
   - 删除内置的两条官方服务器默认项、官方同步入口和相关 UI 文案/状态。
   - 对升级场景清理 App 先前生成的官方服务器缓存，确保旧数据不会继续出现在选择列表中。
   - 保留用户服务器导入、下载和手工编辑能力。

5. Core 国内镜像下载
   - 对 GitHub release 资源构造以下尝试顺序：
     1. `https://ghfast.top/`
     2. `https://gh-proxy.com/`
     3. `https://mirror.ghproxy.com/`
     4. GitHub 原始地址
   - 每个源独立处理重定向、HTTP 状态、连接/读取超时、临时文件和校验；失败后自动尝试下一个源。
   - 在 GUI 与诊断日志中显示当前尝试源和最终错误，避免只报告最后一次异常。

6. 构建与测试
   - 先执行静态编译与 Debug 构建，再生成临时本地签名的 Release/Debug APK。
   - 做安装前 APK 包名、版本、ABI、manifest 权限与签名检查。
   - 在 `10.126.126.66:5555` 进行安装/启动、Root 探测、设置保存、Core 下载 fallback、划掉后台后进程保持等验证。
   - 待 `10.126.126.34:5555` 可连接后，卸载旧签名版本前先按需备份 `/data/data/cn.moonflow.easytier/files/`；安装新包后导入 `/storage/emulated/0/Download/默认配置.toml`，设置 `core_log_level=debug`，验证 Root 模式和重启自启动。

7. 交付与提交
   - 每个阶段只提交 MoonTier 仓库内相关文件：计划、功能实现、验证/构建修正可分开提交。
   - 不修改 `E:\GitRepository\AlasToFox`，不触碰除 `.66` 和 `.34` 之外的任何云手机，不推送远端。
   - 交付已签名 APK、签名方式/指纹、构建命令、测试结果与仍需真机验证的风险项。

## 验收标准

- GUI 中存在明确的开机自启动开关，关闭时无开机拉起行为；开启后 Root 模式可在重启且不打开 App 的情况下恢复连接。
- 无 KernelSU/Magisk/APatch 品牌环境时，只要授权后的 `su` 能返回 UID 0，App 就能识别并执行 Root 流程。
- App 中不再出现或自动恢复两条官方服务器及官方服务器同步入口。
- EasyTier Core 下载按三个国内源顺序失败切换，最后才访问 GitHub 原始地址。
- 构建产物可安装、签名可识别，且所有 ADB 操作都有明确目标序列号。
